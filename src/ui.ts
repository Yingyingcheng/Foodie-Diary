/* Shared Tailwind class strings used across pages (dashboard + goals). */

export const planContainer =
  "max-w-[520px] mx-auto flex flex-col gap-5 pb-10 max-md:px-2 max-md:pb-8";

/* Background color is added per-usage (dashboard cards vs. goals cards differ) */
export const planSection =
  "rounded-[20px] p-6 shadow-[0_2px_12px_rgba(120,60,30,0.12)] max-md:px-3.5 max-md:py-[18px]";

export const planSectionTitle =
  "text-[1.1rem] text-[#6d4030] mt-0 mb-3.5 uppercase tracking-[0.05em]";
